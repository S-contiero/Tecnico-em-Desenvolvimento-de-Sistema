programa {
  funcao inicio() {
    real basemaior, basemenor, altura, area 
    escreva("basemenor:")
    leia(basemaior)
    escreva("basemenor:")
    leia(basemenor)
    escreva("altura:")
    leia(altura)
    area = ((basemaior+basemenor)*altura)/2
    escreva("area do trapezio=",area)
  }
}
